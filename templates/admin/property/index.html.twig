{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}- Accueil{% endblock %}

{% block body %}

<div class="container">
    <!-- Page Content goes here -->

    <h2>Administrer les biens</h2>

    <table class="table striped">
        <thead>
            <tr>
                <th>Nom du bien</th>
                <th>Mettre à jour le bien</th>
                <th>Supprimer le bien</th>
            </tr>
        </thead>
        <tbody>
            {% for property in properties %}
            <tr>
                <td><a href="{{path('property_show', {id: property.id, slug: property.slug }) }}">{{property.title}}</a>
                </td>
                <td><a href="{{path('admin_property_edit', {id: property.id, slug: property.slug }) }}"
                        class="btn">Modifier</a></td>
                <td>{{ include('admin/property/_delete_form.html.twig') }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}