{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}- Accueil{% endblock %}

{% block body %}

<div class="container">
    <!-- Page Content goes here -->
    <div class="row">
        <h4>Administrer les biens</h4>
    </div>

    <div class="row">
        <table class="table striped col s12">
            <thead>
                <tr>
                    <th>Nom du bien</th>
                    <th>Mettre à jour</th>
                    <th>Supprimer</th>
                </tr>
            </thead>
            <tbody>
                {% for property in properties %}
                <tr>
                    <td><a
                            href="{{path('property_show', {id: property.id, slug: property.slug }) }}">{{property.title}}</a>
                    </td>
                    <td><a href="{{path('admin_property_edit', {id: property.id, slug: property.slug }) }}"
                            class="btn">Modifier</a></td>
                    <td>{{ include('admin/property/_delete_form.html.twig') }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>


    <div class="row">
        <div class="navigation row center-align{# col s12 l6 #} {# offset-l4 #}">
            {{ knp_pagination_render(properties) }}
        </div>
    </div>
</div>
{% endblock %}