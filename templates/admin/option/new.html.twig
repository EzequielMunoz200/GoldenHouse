{% extends 'base.html.twig' %}

{% block title %}{{parent()}} - Créer une option{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col s12 l4 offset-l4">
            <div class="row">
                <h4>Créer une option</h4>
            </div>

            <div class="row">
                {{ include('admin/option/_form.html.twig') }}
            </div>
            <div class="row">
                <a href="{{ path('admin_option_index') }}">Retourner à la liste des optiones</a>
            </div>
        </div>
    </div>
</div>

{% endblock %}