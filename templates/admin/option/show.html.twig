{% extends 'base.html.twig' %}

{% block title %}{{parent() }} - Option{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="row">
            <h4>Option</h4>
        </div>
        <div class="row right">
            <a class="valign-wrapper" href="{{ path('admin_option_index') }}"><i
                class="left material-icons">backspace</i>Retourner à la liste</a>
        </div>

        <div class="row">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ option.id }}</td>
                    </tr>
                    <tr>
                        <th>Nom de l'option</th>
                        <td>{{ option.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        
        <div class="row">
            <a href="{{ path('admin_option_edit', {'id': option.id}) }}">Modifier</a>
        </div>

        <div class="row">
            {{ include('admin/option/_delete_form.html.twig') }}
        </div>

    </div>
</div>

{% endblock %}